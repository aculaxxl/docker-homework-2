# Docker Homework

Simple console Python application packaged with Docker and docker-compose.

## Description

The application prints random user data to the console on each run.
It uses the Faker library to generate random data.

## Requirements

- Docker
- Docker Compose
- Make

## Run application

Run the app in Docker with one command:

```bash
make d-homework-i-run
